# Core (wizard + config + task engine work with stdlib only)
python-dotenv>=1.0.0
pydantic>=2.0.0

# API server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
websockets>=12.0

# Voice in (Deepgram streaming)
deepgram-sdk>=3.7.0
sounddevice>=0.4.6
numpy>=1.26.0

# Fast LLMs (all OpenAI-compatible; the `openai` client talks to all three)
openai>=1.30.0
cerebras-cloud-sdk>=1.0.0
groq>=0.9.0

# Idea evaluator / agent chains
langchain>=0.2.0
langchain-openai>=0.1.0

# TUI visualizer (sine waves + spectrogram in the terminal)
textual>=0.70.0
rich>=13.7.0

# Windows toasts (installed only on Windows; see setup_wizard.py)
win10toast-click>=0.1.2; sys_platform == "win32"

# Dev
pytest>=8.0.0
